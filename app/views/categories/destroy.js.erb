document.querySelector("#category-<%= @category.id %>").remove();

<% if @category.parent %>
  <% if @category.parent.is_empty? %>
    var warning = collapsable.querySelector(".empty-category-warning");
    if (!warning) {
      collapsable.insertAdjacentHTML("afterbegin", "<%= escape_javascript( render 'bookmarks/components/category/warning_empty' ) %>");
    }
  <% end %>
<% end %>
